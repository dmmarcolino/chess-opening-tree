# Os PGNs brutos SAO versionados de proposito (nao ignorados) -- a busca
# incremental (--since) depende deles persistirem entre uma execucao do
# workflow e outra. Ignora-los aqui foi um erro que ja causou perda de
# historico: cada execucao do GitHub Actions comecava com data/raw vazia
# (repo nao tem esses arquivos), entao a busca incremental so trazia as
# partidas recentes e sobrescrevia o pouco que tinha, em vez de somar ao
# historico completo. Se um dia quiser reconsiderar o tamanho do repo,
# a forma certa e mudar build_tree.py pra nao depender mais do PGN bruto
# completo a cada execucao -- nao voltar a ignorar esses arquivos aqui.
